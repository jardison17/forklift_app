import 'package:flutter/material.dart';

class ListaEmpilhadeiras extends StatelessWidget {
  const ListaEmpilhadeiras({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [Card(child: ListTile(title: Text('Olá mundo')))]);
  }
}
